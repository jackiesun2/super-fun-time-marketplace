<nav class="nav-container">
  <div class="nav-middle-container">
    <div class="nav-inner-container first">
      <a class="navbar-brand"> <%= link_to "Super Fun Time", root_path, class: 'nav-link active'%> </a>   
    </div>
    <div class="nav-inner-container second">
      <% if user_signed_in? %>
        <%= link_to current_user.profile.first_name, profile_path(current_user.profile)%>
        <a class="nav-buttons"> <%= link_to 'Sign Out', destroy_user_session_path, method: :delete, class: "btn btn-primary" %> </a>
      <% else %>
        <a class="nav-buttons"> <%= link_to 'Sign In', new_user_session_path, class: "btn btn-primary" %> </a>
        <a class="nav-buttons"> <%= link_to 'Sign Up', new_user_registration_path, class: "btn btn-primary" %> </a>
      <% end %>
    </div>
  </div>  
</nav>
